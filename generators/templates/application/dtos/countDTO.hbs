import { type Where{{ pascalCase name }}DTO } from 'domain/dtos/repositories/{{ pascalCase name }}RepositoryDTO';

export abstract class Count{{ pascalCasePlural name }}UseCaseRequestDTO {
  where?: Where{{ pascalCase name }}DTO;
}
