import { Injectable } from '@nestjs/common';

import { type {{ pascalCase name }} } from 'domain/entities/{{ camelCase name }}/{{ pascalCase name }}';
import { {{ pascalCase name }}Repository } from 'domain/repositories/{{ pascalCase name }}Repository';

import { type Deactivate{{ pascalCase name }}UseCaseRequestDTO } from 'application/dtos/{{ camelCase name }}/Deactivate{{ pascalCase name }}UseCaseDTO';

@Injectable()
export class Deactivate{{ pascalCase name }}UseCase {
  constructor(private readonly {{ camelCase name }}Repository: {{ pascalCase name }}Repository) {}

  async execute(request: Deactivate{{ pascalCase name }}UseCaseRequestDTO): Promise<{{ pascalCase name }}> {
    const {{ camelCase name }} = await this.{{ camelCase name }}Repository.deactivate({ where: request.where });

    return {{ camelCase name }};
  }
}
