import { Injectable } from '@nestjs/common';

import { {{ pascalCase name }} } from 'domain/entities/{{ camelCase name }}/{{ pascalCase name }}';
import { {{ pascalCase name }}Repository } from 'domain/repositories/{{ pascalCase name }}Repository';

import { FindFirst{{ pascalCase name }}UseCaseRequestDTO } from 'application/dtos/{{ camelCase name }}/FindFirst{{ pascalCase name }}UseCaseDTO';

@Injectable()
export class FindFirst{{ pascalCase name }}UseCase {
  constructor(private readonly {{ camelCase name }}Repository: {{ pascalCase name }}Repository) {}

  async execute(request: FindFirst{{ pascalCase name }}UseCaseRequestDTO): Promise<{{ pascalCase name }}> {
    const {{ camelCase name }} = await this.{{ camelCase name }}Repository.findFirst({
      where: request.where,
      returnError: request.returnError,
    });

    return {{ camelCase name }};
  }
}
