import { Injectable } from '@nestjs/common';

import { type {{ pascalCase name }} } from 'domain/entities/{{ camelCase name }}/{{ pascalCase name }}';
import { {{ pascalCase name }}Repository } from 'domain/repositories/{{ pascalCase name }}Repository';

import { type Update{{ pascalCase name }}UseCaseRequestDTO } from 'application/dtos/{{ camelCase name }}/Update{{ pascalCase name }}UseCaseDTO';

@Injectable()
export class UpdateMany{{ pascalCasePlural name }}UseCase {
  constructor(private {{ camelCase name }}Repository: {{ pascalCase name }}Repository) {}

  async execute(request: Update{{ pascalCase name }}UseCaseRequestDTO[]): Promise<{{ pascalCase name }}[]> {
    const update{{ pascalCasePlural name }} = await this.{{ camelCase name }}Repository.updateMany(request);

    return update{{ pascalCasePlural name }};
  }
}
