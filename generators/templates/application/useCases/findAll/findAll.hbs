import { Injectable } from '@nestjs/common';

import { {{ pascalCase name }} } from 'domain/entities/{{ camelCase name }}/{{ pascalCase name }}';
import { {{ pascalCase name }}Repository } from 'domain/repositories/{{ pascalCase name }}Repository';

import { FindAll{{ pascalCasePlural name }}UseCaseRequestDTO } from 'application/dtos/{{ camelCase name }}/FindAll{{ pascalCasePlural name }}UseCaseDTO';

@Injectable()
export class FindAll{{ pascalCasePlural name }}UseCase {
  constructor(private readonly {{ camelCase name }}Repository: {{ pascalCase name }}Repository) {}

  async execute(request: FindAll{{ pascalCasePlural name }}UseCaseRequestDTO): Promise<{{ pascalCase name }}[]> {
    const {{ camelCasePlural name }} = await this.{{ camelCase name }}Repository.findAll({
      order: request.order,
      where: request.where,
    });

    return {{ camelCasePlural name }};
  }
}
