import { Injectable } from '@nestjs/common';

import { type {{ pascalCase name }} } from 'domain/entities/{{ camelCase name }}/{{ pascalCase name }}';
import { {{ pascalCase name }}Repository } from 'domain/repositories/{{ pascalCase name }}Repository';
import { RelayPagination } from 'domain/shared/dtos/RelayPagination';

import { type FindAll{{ pascalCasePlural name }}UseCaseRequestDTO } from 'application/dtos/{{ camelCase name }}/FindAll{{ pascalCasePlural name }}UseCaseDTO';

@Injectable()
export class FindAll{{ pascalCasePlural name }}UseCase {
  constructor(private {{ camelCase name }}Repository: {{ pascalCase name }}Repository) {}

  async execute(
    request: FindAll{{ pascalCasePlural name }}UseCaseRequestDTO,
  ): Promise<RelayPagination<{{ pascalCase name }}>> {
    const {{ camelCasePlural name }} = await this.{{ camelCase name }}Repository.findAll({
      last: request.last,
      order: request.order,
      after: request.after,
      first: request.first,
      where: request.where,
      before: request.before,
    });

    return {{ camelCasePlural name }};
  }
}
