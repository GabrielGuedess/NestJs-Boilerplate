import { Injectable } from '@nestjs/common';

import { type {{ pascalCase name }} } from 'domain/entities/{{ camelCase name }}/{{ pascalCase name }}';
import { {{ pascalCase name }}Repository } from 'domain/repositories/{{ pascalCase name }}Repository';

import { type Delete{{ pascalCase name }}UseCaseRequestDTO } from 'application/dtos/{{ camelCase name }}/Delete{{ pascalCase name }}UseCaseDTO';

@Injectable()
export class Delete{{ pascalCase name }}UseCase {
  constructor(
    private {{ camelCase name }}Repository: {{ pascalCase name }}Repository
  ) {}

  async execute(request: Delete{{ pascalCase name }}UseCaseRequestDTO): Promise<{{ pascalCase name }}> {
    const {{ camelCase name }} = await this.{{ camelCase name }}Repository.delete({ where: request.where });

    return {{ camelCase name }};
  }
}
