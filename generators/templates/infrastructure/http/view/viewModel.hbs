import { type {{ pascalCase name }} } from 'domain/entities/{{ camelCase name }}/{{ pascalCase name }}';

import { type {{ pascalCase name }}ResponseDTO } from 'infrastructure/http/dtos/{{ camelCase name }}/{{ pascalCase name }}ResponseDTO';

export class {{ pascalCase name }}ViewModel {
  static toHTTP({{ camelCase name }}: {{ pascalCase name }}): {{ pascalCase name }}ResponseDTO {
    return {
      id: {{ camelCase name }}.id,
      {{#each fields}}
        {{#unless isHideField}}
          {{snakeCase name}}: {{camelCase ../name}}.{{camelCase name}},
        {{/unless}}
      {{/each}}
    };
  }
}
